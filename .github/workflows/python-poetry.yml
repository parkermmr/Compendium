name: Use My Poetry Action
on: [push]

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - uses: parkermmr/compendium/jobs/python/python-poetry-pytest@v1
        with:
          python-version: "3.11"